# Canonical input for the production API lock. Keep apps/api/requirements.txt
# as the API's direct dependency intent; this file also captures the external
# requirements of the repository-root packages installed by the API image.
-r requirements.txt

# Runtime requirements declared by packages/{commons,llm,memory,retrieval}.
# Keep these in sync with those package manifests. tools and agent_runtime have
# no runtime dependencies at this time.
cryptography>=49.0.0
httpx>=0.28.0
motor>=3.7.0
openai>=2.0.0
pydantic>=2.12.0
python-dotenv>=1.1.0
redis>=5.0.0
structlog>=25.4.0
tenacity>=9.0.0

# Build backends used when the local packages are installed with
# --no-build-isolation after the lock is installed.
setuptools>=61.0
wheel
